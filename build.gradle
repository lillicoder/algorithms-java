apply plugin: "java"

sourceCompatibility = 1.8
version = "0.1a"

repositories {
    mavenCentral()
}
	
dependencies {
    testCompile "junit:junit:4.+"
}

task wrapper(type: Wrapper) {
	gradleVersion = "2.3"
}